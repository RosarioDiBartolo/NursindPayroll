How is the comunication between frontend and backend handled? should we use direct tcp/websockets? SSE?
Very important, the use of reducer, i dont like this this way, state managemebnt should fit the use of the chosen solution for server frontend comunication, and we should have one source of truth.
an important feature is the ability to retake the view of a crawling batch of the user, by a path for example, so that we don't lose frontend track

Adding more seprated and explicit commands to login and start and create a sesion and starting the crawl jobs, those steps needs to be separated

Years should not start by 2019, there should't really be a limit, the years should be sent by teh user when starting the crawl by a form field 
general ui ux improvements:

use of proper shadcn components and related.
using more sofisticated and dveeloped forms solutions and validation libraires.
